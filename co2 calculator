<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CO₂ Calculator</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
    body {
    font-family: Arial, sans-serif;
    background-image: url('https://t3.ftcdn.net/jpg/04/39/02/46/360_F_439024603_pHjWAZrHOojIFLykqoG5YBv65zcomlMS.jpg'); /* Use HD image */
    background-size: cover; /* Ensures full-screen coverage */
    background-repeat: no-repeat;
    background-position: center;
    background-attachment: fixed;
    image-rendering: high-quality; /* Improves sharpness */
}
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(0, 50, 0, 0.8);
            padding: 15px 30px;
            border-radius: 10px;
        }
        .navbar a {
            color: white;
            text-decoration: none;
            margin: 0 15px;
            font-size: 16px;
            font-weight: bold;
        }
        .navbar a:hover {
            text-decoration: underline;
        }
        .container {
            width: 90%;
            margin: auto;
            background:rgb(40, 166, 40);
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            flex-wrap: wrap;
        }
        .left, .right {
            flex: 1;
            padding: 15px;
        }
        .section-box {
            background: rgba(255, 255, 255, 0.9);
            color: black;
            padding: 15px;
            border-radius: 10px;
            text-align: left;
            font-size: 14px;
            margin-bottom: 20px;
        }
        .input-group {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin: 10px 0;
            padding: 8px;
        }
        label {
            flex: 1;
            text-align: left;
            font-size: 16px;
            font-weight: bold;
        }
        input {
            flex: 1;
            padding: 8px;
            border-radius: 5px;
            border: 2px solid transparent;
            text-align: center;
            background: #f4f4f4;
            width: 100%;
        }
        input.warning {
            border: 2px solid red;
        }
        button {
            width: 100%;
            margin-top: 15px;
            padding: 12px;
            border: none;
            background:green;
            color: black;
            font-size: 18px;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
        }
        .result {
            margin-top: 20px;
            padding: 10px;
            background: white;
            color: black;
            border-radius: 5px;
            font-size: 16px;
            font-weight: bold;
            text-align: center;
        }
        .graph-container {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            margin-top: 10px;
        }
        canvas {
            background: white;
            border-radius: 5px;
            width: 100%;
            max-width: 400px;
        }
    </style>
</head>
<body>

    <!-- Navigation Bar -->
    <div class="navbar">
        <h2>🌿 CO₂ CALCULATOR</h2>
        <div>
            <a href="#">Profile</a>
            <a href="#">Home</a>
            <a href="#">Contact Us</a>
            <a href="#">Log Out</a>
        </div>
    </div>

    <br>

    <div class="container">
        <!-- Left Section: Limits, Inputs & Results -->
        <div class="left">
            <h3>📏 LIMITS</h3>
            <div class="section-box">
                ⚡ <b>Electricity Usage:</b> 116.25 kWh/Month <br>
                🚗 <b>Car Mileage:</b> 30-40 km/day <br>
                ⛽ <b>Fuel Efficiency:</b> 16.1 KMPL <br>
                🔥 <b>LPG Cylinders:</b> 6 Cylinders / Year <br>
                🗑 <b>Waste Produced:</b> 15 to 35 kg/week
            </div>

            <h2>Enter Your Data</h2>

            <div class="input-group">
                <label>⚡ Electricity Usage (kWh/Month):</label>
                <input type="number" id="electricity" placeholder="Enter kWh">
            </div>

            <div class="input-group">
                <label>🚗 Car Mileage (km/day):</label>
                <input type="number" id="mileage" placeholder="Enter km/day">
            </div>

            <div class="input-group">
                <label>⛽ Fuel Efficiency (KMPL):</label>
                <input type="number" id="fuel" placeholder="Enter KMPL">
            </div>

            <div class="input-group">
                <label>🔥 LPG Cylinders (per year):</label>
                <input type="number" id="lpg" placeholder="Enter cylinders/year">
            </div>

            <!-- Properly aligned Waste Produced Field -->
            <div class="input-group">
                <label>🗑 Waste Produced (kg/week):</label>
                <input type="number" id="waste" placeholder="Enter kg/week">
            </div>

            <button onclick="calculateResult()">Check Result</button>

            <h3>RESULTS</h3>
            <div class="section-box">
                <p id="result">Your results will appear here.</p>
            </div>
        </div>

        <!-- Right Section: Graph -->
        <div class="right">
            <h3>GRAPH</h3>
            <div class="graph-container">
                <canvas id="usageChart"></canvas>
            </div>
        </div>
    </div>

    <script>
        function calculateResult() {
    let electricity = parseFloat(document.getElementById("electricity").value) || 0;
    let mileage = parseFloat(document.getElementById("mileage").value) || 0;
    let fuel = parseFloat(document.getElementById("fuel").value) || 0;
    let lpg = parseFloat(document.getElementById("lpg").value) || 0;
    let waste = parseFloat(document.getElementById("waste").value) || 0;

    const limits = {
        electricity: 116.25,
        mileage: 40,
        fuel: 16.1,
        lpg: 6,
        waste: 35
    };

    let message = "✅ All values are within safe limits!";
    let exceeded = false;

    if (electricity > limits.electricity) {
        message = "⚠ Excess electricity usage detected! Try using LED bulbs or solar energy.";
        exceeded = true;
    }
    if (mileage > limits.mileage) {
        message += "<br>⚠ High car mileage! Use public transport or carpool.";
        exceeded = true;
    }
    if (fuel < limits.fuel) {
        message += "<br>⚠ Low fuel efficiency! Keep tires properly inflated and drive smoothly.";
        exceeded = true;
    }
    if (lpg > limits.lpg) {
        message += "<br>⚠ High LPG consumption! Use energy-efficient stoves and limit unnecessary cooking.";
        exceeded = true;
    }
    if (waste > limits.waste) {
        message += "<br>⚠ Too much waste produced! Reduce, reuse, and recycle.";
        exceeded = true;
    }

    document.getElementById("result").innerHTML = exceeded ? message : "✅ Your usage is within safe limits! Keep up the good work!";
    
    updateGraph([electricity, mileage, fuel, lpg, waste]);
}

function updateGraph(userData) {
    let ctx = document.getElementById('usageChart').getContext('2d');

    if (window.usageChartInstance) {
        window.usageChartInstance.destroy();
    }

    window.usageChartInstance = new Chart(ctx, {
        type: 'pie',
        data: {
            labels: ['Electricity', 'Mileage', 'Fuel', 'LPG', 'Waste'],
            datasets: [{
                data: userData,
                backgroundColor: ['red', 'blue', 'purple', 'orange', 'gray']
            }]
        }
    });
}
</script>

</body>
</html>
