<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carbon Footprint Calculator</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #2E7D32;
            text-align: center;
            color: white;
            margin: 0;
            padding: 20px;
        }
        .container {
            width: 60%;
            margin: auto;
            background: #4CAF50;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }
        .input-group {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin: 10px 0;
        }
        label {
            flex: 1;
            text-align: left;
            font-size: 16px;
        }
        input {
            flex: 1;
            padding: 8px;
            border-radius: 5px;
            border: none;
            text-align: center;
        }
        button {
            margin-top: 15px;
            padding: 12px 20px;
            border: none;
            background: #FFD700;
            color: black;
            font-size: 18px;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
        }
        .result {
            margin-top: 20px;
            padding: 10px;
            background: white;
            color: black;
            border-radius: 5px;
            font-size: 16px;
            font-weight: bold;
        }
        canvas {
            margin-top: 20px;
            background: white;
            border-radius: 5px;
        }
    </style>
</head>
<body>

    <h2>🌱 Carbon Footprint Calculator</h2>
    <div class="container">
        <h3>Enter Your Usage Data</h3>

        <div class="input-group">
            <label>⚡ Electricity Usage (kWh/Month):</label>
            <input type="number" id="electricity" placeholder="Enter kWh">
        </div>

        <div class="input-group">
            <label>🚗 Car Mileage (km/day):</label>
            <input type="number" id="mileage" placeholder="Enter km/day">
        </div>

        <div class="input-group">
            <label>⛽ Fuel Efficiency (KMPL):</label>
            <input type="number" id="fuel" placeholder="Enter KMPL">
        </div>

        <div class="input-group">
            <label>🔥 LPG Cylinders (per year):</label>
            <input type="number" id="lpg" placeholder="Enter cylinders/year">
        </div>

        <div class="input-group">
            <label>🗑 Waste Produced (kg/week):</label>
            <input type="number" id="waste" placeholder="Enter kg/week">
        </div>

        <button onclick="calculateResult()">Check Result</button>

        <div class="result" id="result">Your results will appear here.</div>

        <canvas id="usageChart" width="400" height="200"></canvas>
    </div>

    <script>
        function calculateResult() {
            let electricity = parseFloat(document.getElementById("electricity").value) || 0;
            let mileage = parseFloat(document.getElementById("mileage").value) || 0;
            let fuel = parseFloat(document.getElementById("fuel").value) || 0;
            let lpg = parseFloat(document.getElementById("lpg").value) || 0;
            let waste = parseFloat(document.getElementById("waste").value) || 0;

            // Defined Limits
            const limits = {
                electricity: 116.25,
                mileage: 40,
                fuel: 16.1,
                lpg: 6,
                waste: 35
            };

            let message = "✅ All values are within safe limits!";
            let exceeded = false;

            if (electricity > limits.electricity) {
                message = "⚠ High Electricity Usage! Reduce power consumption.";
                exceeded = true;
            }
            if (mileage > limits.mileage) {
                message += "<br>⚠ High Car Mileage! Try carpooling.";
                exceeded = true;
            }
            if (fuel < limits.fuel) {
                message += "<br>⚠ Low Fuel Efficiency! Maintain vehicle regularly.";
                exceeded = true;
            }
            if (lpg > limits.lpg) {
                message += "<br>⚠ High LPG Usage! Reduce unnecessary consumption.";
                exceeded = true;
            }
            if (waste > limits.waste) {
                message += "<br>⚠ Excess Waste! Follow the 3Rs (Reduce, Reuse, Recycle).";
                exceeded = true;
            }

            document.getElementById("result").innerHTML = exceeded ? message : "✅ Everything is within safe limits! Keep up the good work!";
            
            // Update Graph
            updateGraph([electricity, mileage, fuel, lpg, waste], [limits.electricity, limits.mileage, limits.fuel, limits.lpg, limits.waste]);
        }

        function updateGraph(userData, limitData) {
            let ctx = document.getElementById('usageChart').getContext('2d');

            // Destroy previous chart instance if it exists
            if (window.usageChartInstance) {
                window.usageChartInstance.destroy();
            }

            window.usageChartInstance = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: ['Electricity (kWh)', 'Mileage (km/day)', 'Fuel Efficiency', 'LPG Cylinders', 'Waste (kg/week)'],
                    datasets: [
                        {
                            label: 'Your Usage',
                            data: userData,
                            backgroundColor: ['red', 'blue', 'purple', 'orange', 'gray']
                        },
                        {
                            label: 'Safe Limit',
                            data: limitData,
                            backgroundColor: 'green'
                        }
                    ]
                },
                options: {
                    responsive: true,
                    scales: {
                        y: { beginAtZero: true }
                    }
                }
            });
        }
    </script>

</body>
</html>
